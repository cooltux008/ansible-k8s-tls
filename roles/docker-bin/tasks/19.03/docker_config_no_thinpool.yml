- name: docker_config_no_thinpool
  template:
    src: "{{ docker_bin_version.split('.')[:2] | join('.') }}/daemon_no_thinpool.json.j2"
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: 0644
  notify: restart docker
